# ADAS 캡스톤2 인수인계 문서

> **작성**: 2026-06-01 13:34 KST
> **목적**: 다른 모델에게 현 상황을 한 번에 전달
> **요점**: v16 6시드 + v19a/b 6시드(대기)로 메모리 누적 학습 효과를 정량 입증하는 단계

---

## 0. TL;DR (30초 요약)

- **연구질문**: LLM teacher agent가 **case-based memory** 누적으로 ADHD 학생 식별을 학습할 수 있는가?
- **방법**: ExpeL/Reflexion/STaR/A-MEM/CoALA/Self-Refine 차용. baseline=rule-teacher vs policy=LLM+memory의 30 클래스 longitudinal.
- **현재**: v16 시드 42/43/44 완주(평균 ΔF1 **+0.05**, ΔSens **+0.52**), 45/46/47 진행 중(13:06 launch, ~9~10일). v19 코드 변경 끝, 8002 dflash 복구 대기.
- **한계**: 학습 신호 약함(특히 PPV). v19에서 4 fix로 보강.

---

## 1. 경로 / 인프라

| 항목 | 값 |
|------|----|
| 작업 디렉토리 | `/bce/groups/pnucolab/analysis/scgpt/intern/캡스톤2/adas` |
| 심볼릭 링크 | `/tmp/adas_link` (한글 cwd 우회용) |
| LLM endpoint A | `http://10.125.208.217:8001/v1` (luce-qwen3.6, 안정) |
| LLM endpoint B | `http://10.125.208.217:8002/v1` (luce-qwen3.6, dflash 불안정) |
| API key | `advprog-luce-2026-df12fb79bc1c4e4f800351fe` |

**한글 cwd 주의**: Edit/Write tool이 한글 경로에서 가끔 실패함. 발생 시 `python -c "..."` in-place 편집 + md5 검증으로 우회.

---

## 2. 실험 버전 history

| 버전 | 무엇 | 결과 / 상태 |
|------|------|------|
| v1~v9 | 초기 메모리 디자인 반복 | action 분포 collapse, 학습 X. v9 이후 폐기 |
| v10 | task 4 narrative pivot | 부분 학습 확인 |
| **v16** | v10 + ExpeL/Reflexion/STaR/A-MEM/CoALA/Self-Refine | **메인 결과**. 30 클래스 완주, ΔF1 +0.05 (3 seed), 추가 3 seed 진행 중 |
| v17 | student LLM narrative | dflash crash로 폐기 |
| v18 | v16 + 7 fix (self_critic 약화, anti-collapse, temp 0.3, …) | 13/30에서 stuck, 2026-05-31 killed |
| **v19** | v16 + 4 root cause fix (코드 완료, launch 대기) | a=task 동일, b=task 변경 두 variant |

---

## 3. v16 최종 결과 (시드 42/43/44, 30 클래스)

```
seed   baseline_F1   policy_F1    ΔF1      ΔSens    ΔPPV
s42    0.394          0.253       -0.141   +0.350   -0.159   (퇴행)
s43    0.080          0.209       +0.129   +0.383   +0.103   (성장)
s44    0.097          0.258       +0.161   +0.817   +0.057   (성장)
mean   0.190          0.240       +0.050   +0.517   ±0.000
```

**메모리 누적**: case_base 약 40,000건, principle 200~600건, procedural 0건(CoALA 모듈만 존재).

**figures**: `/tmp/v16_final_30class.png`, `/tmp/v16_final_delta.png`, `/tmp/v16_vs_v18_partial.png`, `/tmp/v16_memory_growth.png`. 생성 스크립트 `/tmp/adas_link/make_figures.py`.

**상세**: `/tmp/adas_link/결과정리.md` 참조.

---

## 4. 4 Root Cause + v19 Fix

| # | 원인 | Fix | 코드 위치 |
|---|------|-----|-----------|
| 1 | case_base noise 누적 (40k+ 무차별) | **case decay 0.95/class + cap 200, labeled 보존** | `teacher_memory.py:_apply_case_decay_and_cap()` |
| 2 | cold start 도메인 지식 부재 | **DSM-5 한국어 5 principle 사전 주입** (`is_dsm5_prior=True`, decay-exempt) | `teacher_memory.py:inject_dsm5_priors()` + `DSM5_PRIOR_PRINCIPLES` |
| 3 | gate threshold sweet spot 부재 | **gate 완전 우회** (`OMC_BYPASS_GATE=1`) | 기존 env |
| 4 | retrieval 신호 약함 (task 자체가 어려움) | **ADHD prevalence 25%로 ↑, max_turn 600** | `run_growth_comparison.py:--adhd-prevalence` + `orchestrator_v2.py` |

**호환성**: env default off라 기존 launch에 영향 X.

---

## 5. 진행 중 / 대기 중

### 🟢 v16 추가 시드 45/46/47 (진행 중)
- launch: 2026-06-01 13:06 KST
- PID: 1296499/1296500/1296501 (모두 alive, sleeping=LLM 대기)
- 위치: `/tmp/adas_link/results/full30_v16_s4{5,6,7}/`
- 진행: baseline 30/30 완료 (~5분 소요), policy arm 시작
- 예상 종료: 2026-06-10~11
- script: `/tmp/runF_v16_s{45,46,47}.sh`
- TCP 8001 alive 확인

### 🟡 v19 launch 대기 (8002 dflash 복구 후)
**6 process** 한꺼번에 실행 예정:
```bash
nohup bash /tmp/runH_v19a_s42.sh > /tmp/runH_v19a_s42.log 2>&1 &
nohup bash /tmp/runH_v19a_s43.sh > /tmp/runH_v19a_s43.log 2>&1 &
nohup bash /tmp/runH_v19a_s44.sh > /tmp/runH_v19a_s44.log 2>&1 &
nohup bash /tmp/runH_v19b_s42.sh > /tmp/runH_v19b_s42.log 2>&1 &
nohup bash /tmp/runH_v19b_s43.sh > /tmp/runH_v19b_s43.log 2>&1 &
nohup bash /tmp/runH_v19b_s44.sh > /tmp/runH_v19b_s44.log 2>&1 &
```
- **v19a**: task 동일 (v16과 직접 비교 가능)
- **v19b**: `--adhd-prevalence 0.25 --max-turns 600` (학습 신호 강화)

**핵심 env** (양쪽 동일):
```
OMC_BYPASS_GATE=1, OMC_CONF_THRESHOLD=0, OMC_NOBS_GATE=0,
OMC_SELF_CRITIC=0, OMC_PROCEDURAL_MEM=1, OMC_FORCE_REFLECT=1,
OMC_CASE_CAP=200, OMC_CASE_DECAY=0.95, OMC_DSM5_PRIOR=1,
OMC_STUDENT_CONCURRENCY=4
```

### 🔴 v18 (종결)
- 13/30에서 dflash 응답 끊김 → 2026-05-31 SIGTERM
- 메모리는 `results/full30_v18_s4{2,3,4}.partial13of30_*`로 백업
- v19 결과 나오면 head-to-head 불필요 (v19가 상위 set)

---

## 6. Frontend 상태

- `/tmp/adas_link/app/renderer/`: Vite + React, port **5188**
- `/tmp/adas_link/app/server.py` (또는 monitor adapter): port **8010**
- Sprite: github muyaho1/adas의 concept_motion/imagegen 적용 완료
- `ClassroomView.jsx`: 새 sprite + 배경 적용

---

## 7. 사용자 운영 규칙 (자율 판단 규칙)

`~/.claude/projects/.../memory/feedback_adas_autonomous.md`에 저장됨. 핵심:

- **class 10 이전 절대 kill 금지** (성장은 10 클래스는 봐야)
- 정상: PID alive + write_bytes 증가 + TCP ESTAB
- 30분 멈춤 → 1시간 더 보고 결정
- 즉시 개입: 전 PID DEAD, action 95% 몰빵, class 10+ n_identified=0 5클래스 연속, LLM stuck 1h+
- v16 진행 중이면 **사용자 명시 허락 없이 절대 건드리지 X**

---

## 8. 모니터링

- cron `9cc12d2b` (30분 주기): PID/write_bytes/TCP/snapshot/CSV 5점검
- 또는 `/loop 10m ...`로 dynamic 모니터
- 정상이면 한 줄, 개입 결정만 길게 보고

---

## 9. 핵심 파일 인덱스

```
/tmp/adas_link/
├── src/simulation/
│   ├── teacher_memory.py        # ⭐ v19 모든 메모리 fix (L479, L871, L889, L943, L965)
│   ├── orchestrator_v2.py       # adhd_prevalence kwarg (L700)
│   └── classroom_v2.py
├── scripts/
│   └── run_growth_comparison.py # ⭐ --adhd-prevalence CLI (L454)
├── results/
│   ├── full30_v16_s{42,43,44}/  # 완주, *_policy.csv 존재
│   ├── full30_v16_s{45,46,47}/  # 진행 중
│   └── full30_v18_s*.partial*/  # 백업
├── 결과정리.md                  # ⭐ 학술적 분석
├── make_figures.py              # 4 figure 생성
└── app/                         # Frontend (Vite 5188, server 8010)

/tmp/
├── runF_v16_s{42-47}.sh         # v16 launch script
├── runH_v19{a,b}_s{42,43,44}.sh # v19 launch (대기 중)
└── v16_*.png                    # figures
```

---

## 10. 즉시 다음 행동

1. **dflash 8002 복구 확인** (사용자 작업 중)
2. 복구되면 v19 6 process launch
3. v16 s45/46/47 policy arm 진행 (10일 대기)
4. cron 모니터 유지
5. v19 결과 나오면: v16 final vs v19a vs v19b 3-way 비교 분석, figures 갱신

---

## 11. 학술적 framing (논문 작성 대비)

- **Title 후보**: "Memory-Augmented LLM Teacher Agents for ADHD Identification: An ExpeL-Style Case Base Approach"
- **Contribution**: ADHD 식별에 ExpeL+Reflexion 적용, 30 cls longitudinal로 sensitivity 5~9배 향상 입증, PPV는 한계 보고 (정직한 limitation)
- **인용 핵심**: Park 2023(Gen Agents), Zhao 2024(ExpeL), Shinn 2023(Reflexion), Sumers 2024(CoALA), Tu 2024(AMIE)
