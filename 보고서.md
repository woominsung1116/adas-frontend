# 개발 보고서 — ADAS (ADHD Agent-based classroom Simulator)

**작성일**: 2026년 4월 14일
**작성 대상**: `adas` (고급 프로그래밍 과목 캡스톤 프로젝트)
**정리 기준**: 현재 코드/검증 결과 기준. 이때까지 이 프로젝트에서 구현한 모든 것을 정리.

---

## 1. 작업 개요

이번 기간에는 **ADAS** 한 프로젝트를 처음부터 구현했다.

- `adas`: 한국 초등학교 교실에서 AI 교사 에이전트가 20명 학생(정상+ADHD+혼동 프로파일)을 1년(950턴) 동안 관찰하며, 메모리를 축적해 ADHD를 판별하고 케어하는 에이전트 기반 시뮬레이션. Electron 데스크톱 앱으로 실시간 시각화.

이번 보고서의 목적은 내가 진행한 전체 구현을 한 번에 정리해 제출 가능한 형태로 만드는 것이다.

---

## 2. 프로젝트 개요

### 프로젝트 배경

DSM-5 기반 ADHD 진단은 관찰과 구조화된 평가에 의존하지만, 교사의 교실 판별 기반 선별(screening)은 한국에서도 주요 관문이다(K-ARS 교사판). 그러나 실제 교사가 일 년 동안 같은 학급을 겪으며 "어떤 학생이 ADHD인지"에 대한 판단을 갱신해가는 과정은 직접 관찰하기 어렵다. ADAS는 그 과정을 시뮬레이션으로 재현하고, 교사 에이전트가 **학급을 거듭할수록 판별이 빨라지는지**를 실험적으로 검증하기 위한 프로젝트다.

### 기본 구조

ADAS는 Stanford Generative Agents(perceive → retrieve → reflect → plan → act)의 인지 아키텍처와 Agent Hospital(MedAgent-Zero)의 memory 구조를 차용한 **에이전트 기반 교실 시뮬레이션**이다.

- **학생 에이전트 (20명)**: 같은 인지 아키텍처 + 프로파일별 파라미터 차이(dimensional delta)로 ADHD/정상/혼동 프로파일 모델링
- **교사 에이전트 (1명)**: 5-Phase 전략(관찰→스크리닝→판별→케어→유지)에 따라 관찰·개입·판별. Case Base + Experience Base 2-tier 메모리
- **시간 축**: 한국 교육법 기준 1년 = 190일 × 5교시 = 950턴
- **평가**: K-ARS, DSM-5 ADHD 진단 기준, 한국 역학 데이터와 비교

### 기술 스택

| 영역 | 기술 |
|---|---|
| 시뮬레이션 코어 | Python 3.13, numpy, dataclass-based domain model |
| 패키지 관리 | `uv` |
| 테스트 | pytest (현재 854 tests) |
| LLM 추론 | 로컬 LLM (Qwen/Llama via OpenAI-compatible HTTP) + Codex CLI + Claude Code CLI + mock backend |
| 실험/분석 | matplotlib, CSV/JSON 파일 기반 영구화, 자체 paired sign test |
| 데스크톱 앱 | Electron + Vite + React + Phaser 3 (게임 맵 렌더링) |
| 서버 | FastAPI + WebSocket (`uvicorn`) |

---

## 3. 전체 구현 내용

ADAS는 Phase 1A부터 Phase 6 slice 35까지 순차적으로 쌓아 올린 프로젝트다. 구현 내용을 큰 축으로 정리하면 다음과 같다.

### 3.1 Phase 1 — 기본 에이전트 및 프로파일 구조

#### 1. 학생 인지 에이전트 (`src/simulation/cognitive_agent.py`, ~1,700줄)

Stanford Generative Agents 아키텍처:

```
perceive(지각) → retrieve(회상) → reflect(반성) → plan(계획) → act(행동)
```

ADHD는 별도 코드가 아니라 **같은 아키텍처의 파라미터 차이**로 모델링했다. 이것이 이 프로젝트의 설계 철학이다.

| 파라미터 | 정상 | ADHD 부주의 | ADHD 과잉행동 | 불안 | ODD |
|---|---|---|---|---|---|
| att_bandwidth (주의 폭) | 3 | 1 | 2 | 2 | 2 |
| impulse_override (충동) | 0.05 | 0.15 | 0.40 | 0.10 | 0.35 |
| plan_consistency (계획 이행) | 0.90 | 0.50 | 0.40 | 0.75 | 0.40 |
| retention (기억 유지) | 5 | 2 | 3 | 4 | 4 |

#### 2. 18개 프로파일 (Phase 1A + 1B)

`BASE_COGNITIVE + delta` 구조로 관리되는 dimensional delta architecture. `normal_quiet`을 baseline으로 두고 다른 프로파일은 additive delta로 표현. 공존 프로파일은 `_base` field로 재귀 합성 + 명시적 interaction term.

- **단일 (9개)**: normal_quiet, normal_active, adhd_inattentive, adhd_hyperactive_impulsive, adhd_combined, anxiety, odd, gifted, sleep_deprived
- **공존 (6개)**: adhd_i_plus_anxiety, adhd_h_plus_odd, adhd_c_plus_odd, adhd_i_plus_ld, adhd_plus_depression, anxiety_plus_depression
- **감별진단 distractor (3개)**: asd_like, depression, learning_disorder

각 프로파일에 다음 필드를 완비했다.
- `cognitive` / `emotional` / `observable` delta dict
- `_base`: 재귀 합성을 위한 base profile 리스트 (공존용)
- `_sources`: 문헌 출처 주석 (MTA 1999, Jensen 2001, Burke 2002, DuPaul 2013, etc.)
- `_ranges`: autoresearch 탐색 범위 (각 delta 파라미터별 [lo, hi])

#### 3. 8차원 감정 모델

학생 감정: `frustration`, `shame`, `anxiety`, `anger`, `loneliness`, `excitement`, `trust_in_teacher`, `self_esteem`

감정은 행동과 인지 파라미터 양쪽에 영향을 준다. 예: `frustration > 0.5`면 `att_bandwidth` 감소, `anxiety > 0.5`면 `vision_r` 감소 + `importance_trigger` 감소.

#### 4. 관찰 상태 분리 (Phase 1A 버그 수정)

기존에는 모든 학생이 동일한 `{attention: 0.65, compliance: 0.72}`로 시작하던 문제. 수정 후 ADHD-combined는 `attention=0.25, compliance=0.43`으로 프로파일별 초기값 분리.

### 3.2 Phase 2 — 교실 환경 및 상황 modulator

#### 1. 950턴 교실 환경 (`src/simulation/classroom_env_v2.py`, ~2,000줄)

- 1턴 = 1교시, 1학급 = 190일 × 5교시 = 950턴 (한국 교육법 기준)
- 20명 학생: ADHD 6-11% + 혼동 프로파일 15-25% (랜덤 샘플링)
- 학생 간 관계 그래프: 친구/갈등/좌석 인접
- InteractionEngine: 또래 전염, 수다, 충돌, 괴롭힘, 도움
- 부분 관찰: 교사는 턴당 3명만 상세 관찰, 나머지는 겉보기만
- 시간 경과: 교과목 변경, 쉬는시간/체육 시 운동장 이동
- 관리 기준: compliance ≥ 0.80 for 25턴 연속 (재발 시 리셋)

#### 2. 교실 아키타입 6종

- `quiet_structured`: 조용하고 구조화된 교실
- `chaotic`: 소란스러운 교실 (peer conflict rate +10%)
- `exam_period`: 시험 기간, 스트레스 높음
- `high_ses`: 교육열 높은 지역, 자원 풍부
- `low_ses`: 교육 자원 부족 지역 (prevalence +2%, 지원 없음)
- `mixed_grade`: 복식 학급

#### 3. Situational Modulator (`src/simulation/situational_modulator.py`, ~340줄)

문헌 기반 상황 modulation — 교실 역동을 현실적으로 만들기 위한 환경 요인 9종.

- 학기 주기 (Cassady 2002), 학기 말 피로 (Lim 2016)
- 일중 리듬: Post-lunch dip (Folkard 1975)
- 신학기 적응: 100턴 선형 감쇠 (Akos 2004)
- 또래 갈등 (Eisenberger 2003), 대체 교사 (Clifton 1987), 발표 이벤트 (Stein 1996)
- 날씨 (Denissen 2008, 작은 효과만)

`ClassroomEnvironmentV2.step()`에 통합되어 매 턴 학생 state에 modulation 적용. 25 tests passed.

#### 4. Parameter Range YAML (`.harness/student_parameter_ranges.yaml`)

Autoresearch 탐색 공간 정의:
- 10개 cognitive 파라미터 range
- 8개 emotional 파라미터 range
- 프로파일별 delta range (ADHD-I, ADHD-H)
- Emotion decay rates, event reactivity coefficients
- Emotion→cognition coupling
- Comorbidity interaction sparsity weights
- Hard constraints (단조성, 우선순위)

학생(autoresearch 대상) vs 교사(문헌 고정)의 명확한 구분.

### 3.3 Phase 3 — 평가 및 교사 에이전트 고도화

#### 1. Calibration Targets

- `.harness/naturalness_targets.yaml`: 논문 15개 패턴 매칭 (Tier 1)
- `.harness/epidemiology_targets.yaml`: 유병률, 공존률 12개 anchor (Tier 2)
- `.harness/held_out_targets.yaml`: 검증용 Tier 3

#### 2. Calibration Loss (`src/calibration/loss.py`)

3-Tier 구조 loss 함수:
- Tier 1 (60%): Naturalness patterns
- Tier 2 (40%): Epidemiological anchors
- Tier 3 (held-out): loss에 미포함, 검증 전용

`compute_combined_loss(sim_result, naturalness_targets, epidemiology_targets, interaction_terms, ...)` — 스칼라 + 브레이크다운 반환. Sparsity penalty로 comorbidity interaction term L1 정규화.

20 tests passed.

#### 3. 교사 에이전트 v2 — 7차원 감정

rule-based 교사 감정 모델:

```python
BASE_TEACHER_EMOTIONAL = {
    "emotional_exhaustion":    0.42,  # MBI-EE / 이영만 2012
    "depersonalization":       0.30,  # MBI-DP / 조환이 2016
    "personal_accomplishment": 0.72,  # MBI-PA / 한국 교사
    "self_efficacy":           0.72,  # TSES / Klassen 2009
    "empathy":                 0.76,  # Goroshit 2016
    "patience":                0.72,  # Schnitker 2012
    "job_stress":              0.62,  # Kyriacou / 한국
}
```

설계 원칙:
- 초기값: 한국 교사 normative data에서 문헌 고정 (autoresearch X)
- 변화: 100% rule-based 이벤트 카탈로그 (15개), 각 계수 논문 인용
- LLM: 감정 읽기만 (의사결정 context로), 감정 쓰기는 금지
- 재현성: 동일 입력 → 동일 출력 보장

#### 4. 5-Phase 교사 전략

```
Phase 1 (Turn 1-100):   순수 관찰 — 학생 이름 익히기, 전체 훑기
Phase 2 (Turn 100-300): 스크리닝 — 의심 학생 집중 관찰, 가설 검증 루프
Phase 3 (Turn 300-475): 판별 — confidence ≥ 0.85 & 10회+ 관찰 시 DSM-5 리포트
Phase 4 (Turn 475-700): 케어 — 개입 전략 실행
Phase 5 (Turn 700-950): 유지 — 재발 감지 + 재개입
```

#### 5. 가설-검증 루프 (HypothesisTracker)

즉시 판별 대신 `suspicious → 3종 개입 테스트 → 반응 패턴 분석 → 감별` 구조.

- `empathic_acknowledgment` → distress 감소 크면 → 불안장애 가능성
- `break_offer` → 행동 개선 → ADHD 가능성
- `firm_boundary` → 반항 증가 → ODD 가능성
- 3종 테스트 완료 후에만 판별 허용

### 3.4 Phase 4 + 4.5 — Memory + Autoresearch

#### 1. 교사 메모리 (`src/simulation/teacher_memory.py`, ~1,200줄)

Agent Hospital의 MedAgent-Zero 스타일:

- **Case Base**: 관찰 기록 저장, 코사인 유사도 RAG 검색, 최대 3,000건 저장 (labeled 우선 보존)
- **Experience Base**: 성공/실패에서 원칙 추출, 검증 후 보존 (`support_count` 기반 promotion gating)
- **CrossClassMetrics**: `classes_seen`, `total_identifications`, `correct_identifications`, `false_positives`, `false_negatives`
- 학급이 바뀌어도 메모리 유지 → 갈수록 판별 정확도 향상

#### 2. 지연 피드백

- 판별 후 30%만 ground truth 확인 가능 (`feedback_rate=0.30`)
- 70%는 맞았는지 모름 → Experience Base 수정이 느림

#### 3. DSM-5 판별 리포트 (`src/eval/identification_report.py`)

- 부주의 9개 + 과잉행동-충동 9개 = 18개 기준
- 관찰된 행동을 DSM-5 기준에 매핑
- 6개 이상 충족 시 판별 리포트 생성
- `IdentificationEvaluator`로 정확도/subtype 평가

#### 4. 성장 평가 (`src/eval/growth_metrics.py`, ~800줄)

- 민감도, 특이도, PPV, F1, false positive rate
- 한국 벤치마크와 비교 (K-ARS 민감도 >94%, Vanderbilt PPV 0.19)
- Growth curve 시계열 — 학급이 진행될수록 개선되는지 추적
- CSV/JSON export

#### 5. Autoresearch 인프라 (Phase 4.5, `src/calibration/`)

- `orchestrator.py`: multi-start autoresearch loop
- `constraints.py`: 단조성/우선순위 제약 적용
- `applier.py`: 탐색된 파라미터를 시뮬레이션에 적용
- `validation.py`: held-out cross-scenario validation
- `prior_predictive.py`: 사전 예측 체크
- `metrics.py`: calibration quality metrics

### 3.5 Phase 6 — Realism + Honesty Pass (slices 1-19)

설계 의도는 좋으나 실제 동작이 부족하던 부분을 slice 단위로 하나씩 닫았다.

#### 1. 교사 관찰 경계 명시화 (slice 1-4)
- 기존에 `state_snapshot` 덤프로 latent 값을 직접 읽던 경로 제거
- `TeacherObservationBatch` DTO 추가 — 관찰 가능한 행동만 관리
- 케이스 베이스 커밋 시 latent scalar 대신 명시적 `ObservationOutcome` 피드백 페이로드 사용
- Working suspicion set semantics 명시화

#### 2. 교사 인지 노이즈 (slice 5)
- `TeacherNoiseConfig`: dropout + confusion 두 축
- 전용 RNG로 재현성 보장
- 기본 no-op으로 backward compat

#### 3. 지연 피드백 큐 (slice 3, 6)
- `FeedbackDelayQueue`: 턴 지연 후 기록 반영
- 메모리 결정이 "방금 턴 결과"가 아니라 "과거 턴에서 성숙한 결과"로 업데이트
- 가설 검증 피드백도 동일한 delay 정책 적용

#### 4. 행동 유도 mood / incident load (slice 7-8)
- `class_mood`가 latent 값 참조하던 것을 관찰 가능한 행동만으로 재계산
- 교사 감정 업데이트도 관찰 기반 incident load를 입력으로 사용

#### 5. 메모리 retrieval 노이즈 (slice 9-12)
- `RetrievalNoiseConfig`: 교사 메모리 검색이 imperfect
- Calibration helper paths까지 전파
- Observability: summary/report/checkpoint artifact에 노이즈 설정 노출

#### 6. Checkpoint/Resume (slice 13-14)
- `TeacherNoiseConfig` 상태를 calibration resume 시 복원
- 관찰/노이즈 설정이 resume 경로에서도 유지되도록

#### 7. 학생 perceive 루프 연결 (slice 15-16)
- 기존 `current_events=[]`로 비어 있던 입력 채움
- 실제 교실 이벤트 → 학생 인지 루프 배선
- `identify_adhd` 이벤트 필터링으로 학생이 자기 진단 정보를 받지 않도록 sanitize

#### 8. 관찰 가능 mood 전파 (slice 17-19)
- 학생 class mood → 교사 observation 계층도 행동 기반으로 재계산
- `StudentSummary`가 observable-only 되도록 정리

### 3.6 Phase 6 — Backend/Experiment Pipeline (slices 20-27)

Rule-based 실험을 end-to-end로 돌릴 수 있는 인프라 완성.

#### 1. Effective_params 정직화 (slice 20)
- 0410 status report에서 지적한 "7개 중 4개는 실제로 작동 안 함" 문제 해결
- `vision_r`, `importance_trigger` 등 실제로 동작하도록 연결

#### 2. Memory-aware early identification policy (slice 21)
- 5-Phase 엄격한 gate를 완화하는 선택적 정책
- `enable_early_identification=True`일 때만 활성
- 조건: 가설 검증 완료 + 충분한 관찰 + case base signal 확보
- 보수적 설계로 false positive 방지

#### 3. 구조화된 early-ID 메타데이터 (slice 22)
- `TeacherAction.is_early_identification`
- `IdentificationReport.early_identification_turn`, `identification_path`
- `_StudentTrack`에 per-student 기록
- Real-loop integration tests 추가

#### 4. ClassMetrics 집계 (slice 23)
- `n_early_identifications`, `avg_early_identification_turn`, `early_identification_rate`
- `n_phase3_identifications`, `avg_phase3_identification_turn`

#### 5. Suspicion→Identification delta (slice 24)
- Per-student: `first_suspicion_turn`, `suspicion_to_identification_delta`
- Per-class: `avg_suspicion_to_identification_delta`
- Growth curve 시계열에 포함

#### 6. Growth comparison harness (slice 25)
- `scripts/run_growth_comparison.py`: baseline vs policy 비교 runner
- 동일 seed로 양쪽 arm 실행
- CSV + JSON 결과 생성
- Smoke-friendly 기본값 (3 classes × 5 students × 100 turns)

#### 7. 시각화 (slice 26)
- `scripts/plot_growth_comparison.py`: 6-panel matplotlib figure
  - Sensitivity, PPV, Avg ID turn, S→ID delta, Early-ID rate, Early IDs per class
- 조기 판별 발생 클래스를 오렌지 마커로 강조

#### 8. 통계 분석 (slice 27)
- `scripts/analyze_growth_comparison_stats.py`: paired sign test
- scipy 없이 `math.comb`으로 exact binomial 직접 구현
- Per-metric 방향/유의성 리포트

### 3.7 Phase 6 — 30학급 실제 실험 (2026-04-12)

Rule-based 교사 × 20명 학생 × 30학급 × 950턴 × 2 arms 완주 (~4분).

**결과 (mean across 30 classes):**

| Metric | Baseline | Policy | Delta |
|---|---|---|---|
| Sensitivity | 0.583 | 0.650 | +0.067 |
| PPV | 0.140 | 0.141 | +0.001 |
| F1 | 0.220 | 0.223 | +0.003 |
| Avg ID turn | 264.6 | 262.0 | −2.7 |
| S→ID delta | 176.1 | 173.5 | −2.7 |
| Early-ID rate | 0.000 | 0.019 | +0.019 |

**최종 학급 (class 30):**
- Sensitivity: 0.500 → 1.000 (+0.500)
- Identification turn: 306.7 → 280.9 (−25.8턴)
- Early-ID rate: 0.000 → 0.125

**통계적 유의성**: sign test 결과 `early_identification_rate`만 marginal(p=0.0625). Baseline과 policy가 14학급까지 동일하게 돌았기 때문에(early-ID가 아직 안 fire) effective N이 작음.

**해석**: 정책의 보수적 설계(case_base_records 기반 gate)대로 작동. 조기 판별은 case base에 충분한 labeled evidence가 쌓인 후에만 발동. 최종 학급 결과는 정책이 방향적으로 유리함을 보여주지만, 더 많은 학급이나 상세 메트릭(Wilcoxon signed-rank)이 통계적 유의성 확보에 필요.

### 3.8 Phase 6 — 프로파일 완성도 + Robustness (slices 28-29)

#### 1. Phase 1B 프로파일 완성도 (slice 28)
- 18개 프로파일 모두 `_sources`와 `_ranges` 완비
- Default 값 검증: 모든 default가 `_ranges` 안에 떨어지는지 테스트

#### 2. Robustness 스크립트 (slice 29)
- `scripts/run_robustness_checks.py` 2-mode:
  - `sensitivity`: `BASE_COGNITIVE`의 4개 파라미터 (`att_bandwidth`, `importance_trigger`, `plan_consistency`, `impulse_override`) ±20% 스윕
  - `cross_scenario`: 6개 교실 아키타입에서 동일 설정 반복
- CSV/JSON 출력 + stability 통계(mean/std/min/max)

### 3.9 Phase 6 — 로컬 LLM 통합 (slices 30-35)

이관 예정인 로컬 LLM 서버(Qwen/Llama 27B)에 대비해 백엔드/프롬프트/메모리 persistence까지 전부 사전 정비.

#### 1. LocalLLMBackend (slice 30)
- `src/llm/local_llm.py`
- OpenAI-compatible `/v1/chat/completions` 엔드포인트 대상
- Ollama/vLLM/llama.cpp 모두 동일 contract
- `urllib.request`만 사용 (추가 의존성 없음)
- 연결/malformed/empty content 에러 명시적 분기
- `ResponseCache` 통합

#### 2. Backend factory (slice 31)
- `create_backend(name, **kwargs)` 함수
- 지원: `claude_cli`, `codex_cli`, `local_llm`, `mock`
- `inspect.signature` 기반 kwarg 필터링

#### 3. Experiment runner 배선 (slice 32)
- `--teacher-backend`, `--teacher-model`, `--teacher-base-url`, `--teacher-timeout`, `--teacher-temperature`
- `create_backend`로 인스턴스 생성 후 `OrchestratorV2`에 주입

#### 4. 프롬프트 스타일 분기 (slice 33)
- `TeacherLLM(prompt_style="default"|"local_clean")`
- `local_clean`: 27B 급 로컬 모델용 (JSON-only 강제, 반복 framing 제거, 태그 마커)
- 기본 스타일은 Codex/Claude용으로 유지

#### 5. TeacherMemory persistence (slice 34)
- `to_dict()` / `load_dict()` / `save(path)` / `TeacherMemory.load(path)`
- Case Base records + Experience Base principles + cross-class metrics + pending principles 직렬화
- JSON 파일 기반, 버전 필드 + 부분 payload 호환
- CLI: `--memory-load`, `--memory-save`

#### 6. 메모리 wiring 버그 수정 (slice 35)
- `OrchestratorV2`가 `TeacherLLM`을 먼저 만들고 나중에 `orch.memory = loaded_memory`로 덮어쓰던 경로 발견
- LLM 교사의 `teacher_llm.memory`가 원래 empty memory를 가리키는 stale reference 발생
- `OrchestratorV2.__init__(memory=...)` 파라미터 추가 → TeacherLLM 생성 전에 memory 주입
- Rule-based 경로에는 영향 없었음, LLM 경로만 블로커

---

## 4. 데이터 모델 및 아키텍처 구현

### 4.1 Domain Model

핵심 dataclass로 구성된 도메인 레이어:

- `CognitiveParameters` (학생 인지 파라미터 10개)
- `EmotionalState` (학생 감정 8차원)
- `ClassroomArchetype` (교실 아키타입 6종)
- `StudentObservation` / `ClassroomObservation` / `TeacherObservationBatch` (부분 관찰 계층)
- `TeacherAction` (교사 행동 7종 + 전략 12종)
- `PhaseConfig` (교사 5-Phase 경계 + early-ID 정책)
- `HypothesisTracker` (Phase 2 가설 검증)
- `_StudentTrack` (per-class per-student accumulator)
- `IdentificationReport` (DSM-5 structured report)
- `ClassMetrics` (per-class 통계)
- `ObservationRecord` / `ObservationOutcome` (Case Base 기본 단위)
- `Principle` (Experience Base 기본 단위)
- `CrossClassMetrics` (cross-class 통계)

### 4.2 레이어 구조

```
OrchestratorV2 (per-class 실행 루프)
  ├─ ClassroomV2 (학생 × 20, 아키타입, scheduling)
  ├─ TeacherMemory (Case Base + Experience Base + cross-class metrics)
  ├─ TeacherLLM (선택적, LLM 백엔드 + 프롬프트)
  ├─ IdentificationEvaluator (DSM-5 기반 평가)
  └─ GrowthTracker (ClassMetrics 누적)

LLM Layer
  ├─ LLMBackend (abstract)
  ├─ LocalLLMBackend (OpenAI-compatible HTTP)
  ├─ CodexCLIBackend (subprocess + JSON schema)
  ├─ ClaudeCodeBackend (subprocess)
  ├─ MockTeacherBackend (deterministic)
  └─ create_backend(name, **kwargs) (factory)

Calibration Layer (Phase 4.5)
  ├─ autoresearch orchestrator (multi-start)
  ├─ loss (3-tier)
  ├─ constraints (단조성, sparsity)
  ├─ applier (파라미터 적용)
  └─ validation (held-out)
```

### 4.3 인터페이스 / 스트리밍

`stream_class()` 제너레이터 — 실시간 스트리밍 지원:

```python
for event in orch.stream_class():
    if event["type"] == "turn":
        # WebSocket으로 즉시 전송
    elif event["type"] == "class_complete":
        # 학급 종료, 메트릭 포함
```

Pause/resume는 실제 시뮬레이션을 멈추고 재개 (replay가 아님).

---

## 5. Electron 데스크톱 앱

### 구조

```
app/
├── electron/main.js          # Python 백엔드 자동 실행, venv 탐색
├── backend/server.py         # FastAPI WebSocket (classic/multi/v2 모드)
└── renderer/src/
    ├── App.jsx               # 모드 분기, 상태 관리
    └── components/
        ├── ClassroomView.jsx  # Phaser 통합 학교 맵 + 캐릭터 스프라이트
        ├── StudentGrid.jsx    # 20명 학생 상태 그리드
        ├── StatePanel.jsx     # 집중 학생 상태 바 + 교사 행동
        ├── ChatLog.jsx        # 상호작용 로그 (한국 이름, 색상)
        ├── ControlPanel.jsx   # 모드/속도/일시정지 제어
        └── GrowthPanel.jsx    # 성장 트렌드 표시
```

### 학교 맵 (Phaser 3)

```
┌─────────────────────┬──────────┐
│    교실 (Classroom) │ 교무실   │  ← 픽셀아트 이미지
│                     │ (Office) │
├─────────────────────┴──────────┤
│           복도 (Hallway)       │
├────────────────────────────────┤
│        운동장 (Playground)     │  ← 픽셀아트 축구장
└────────────────────────────────┘
```

- 캐릭터 21명 (교사 1 + 학생 20) 걷기 애니메이션
- 시나리오에 따라 교실↔운동장 이동
- 활성 공간 하이라이트, 비활성 공간 딤

### WebSocket 프로토콜 (v2 모드)

```
Client → Server: {"type": "start_session", "mode": "v2", "n_students": 20}

Server → Client:
  1. {"type": "new_class", "class_id": 1, "students": [...], ...}
  2. {"type": "turn", "turn": 1, "day": 1, "period": 1, "subject": "math", ...}
  3. {"type": "turn", "turn": 2, ...}
  ... (950 turns)
  N. {"type": "class_complete", "metrics": {...}, "reports": [...]}

Client → Server (제어):
  {"type": "pause"}
  {"type": "resume"}
  {"type": "speed", "delay": 0.5}
```

실시간 스트리밍: `stream_class()` 제너레이터가 턴마다 yield → 서버가 즉시 전송.

---

## 6. 실험 파이프라인

### 6.1 실험 스크립트

```
scripts/
├── run_growth_comparison.py           # baseline vs policy N학급 비교
├── plot_growth_comparison.py          # 6-panel 시각화
├── analyze_growth_comparison_stats.py # paired sign test
├── run_robustness_checks.py           # sensitivity + cross-scenario
├── run_e2e_test.py                    # mock LLM 2학급 E2E
├── run_codex_demo.py                  # Codex CLI 10명 × 5턴 데모
├── run_experiment.py                  # 기존 실험 러너
└── visualize_results.py               # 기존 결과 시각화
```

### 6.2 실험 실행 예시

```bash
# 1. Rule-based 30학급 비교 (2026-04-12 실행 완료)
.venv/bin/python scripts/run_growth_comparison.py \
  --n-classes 30 --n-students 20 --seed 42 \
  --output-dir results/growth_30class_2026-04-12 --prefix exp30

# 2. 시각화
.venv/bin/python scripts/plot_growth_comparison.py \
  --baseline-csv results/growth_30class_2026-04-12/exp30_baseline.csv \
  --policy-csv   results/growth_30class_2026-04-12/exp30_policy.csv \
  --output-dir   results/growth_30class_2026-04-12/figures

# 3. 통계 분석
.venv/bin/python scripts/analyze_growth_comparison_stats.py \
  --baseline-csv results/growth_30class_2026-04-12/exp30_baseline.csv \
  --policy-csv   results/growth_30class_2026-04-12/exp30_policy.csv \
  --output-dir   results/growth_30class_2026-04-12/stats

# 4. Robustness
.venv/bin/python scripts/run_robustness_checks.py sensitivity --smoke
.venv/bin/python scripts/run_robustness_checks.py cross_scenario --smoke

# 5. 로컬 LLM 교사 (이관 후 예정)
.venv/bin/python scripts/run_growth_comparison.py \
  --teacher-backend local_llm \
  --teacher-model qwen2.5:27b \
  --teacher-prompt-style local_clean \
  --n-classes 3 --n-students 10 \
  --memory-load results/teacher_memory_seed.json \
  --memory-save results/teacher_memory_v2.json
```

---

## 7. 데이터 소스

`data.md`에 전체 정리. 한국 전용:
- 역학 연구 8편 (49,573명 유병률 등)
- 교실 행동 관찰 6편 (메타분석)
- 한국 평가 도구 11개 (K-ARS, K-CBCL 등)
- 개입 연구 10편
- 사례연구 44편

---

## 8. 검증 결과

### 8.1 자동화된 검증

- `pytest tests/` → **854 tests collected**
- 주요 카테고리:
  - 시뮬레이션 코어: test_cognitive_agent, test_classroom_env_v2, test_situational_modulator 등
  - 교사 메모리: test_teacher_memory, test_teacher_observable_feedback, test_teacher_memory_feedback, test_teacher_memory_persistence
  - 교사 감정/소음: test_teacher_emotion, test_teacher_noise, test_retrieval_noise
  - 오케스트레이터: test_v2_orchestrator, test_v2_server, test_v2_integration
  - Calibration: test_calibration_loss, test_calibration_constraints, test_calibration_validation
  - 실험 인프라 (최근): test_effective_params_smoke, test_early_identification_policy, test_class_metrics_early_identification, test_suspicion_to_identification_delta, test_growth_comparison, test_plot_growth_comparison, test_growth_comparison_stats, test_profile_phase1b, test_robustness_checks, test_local_llm_backend, test_backend_factory, test_growth_comparison_backend_wiring, test_teacher_prompt_style, test_teacher_memory_persistence, test_memory_wiring_fix
- Phase 6 slices 20-35에서만 **126개 신규 테스트** 추가, 전부 pass

### 8.2 실제 실험 실행 검증

**30학급 rule-based growth comparison** (2026-04-12):
- 출력: CSV 2개(baseline/policy), JSON 1개(comparison), PNG 1개(6-panel figure), MD 1개(paired stats report)
- 실행 시간: 약 4분 (2 × 30학급 × 950턴)

### 8.3 Electron 앱 실행 검증

**2026-04-14 실행 확인**:
- Python 백엔드 `uvicorn on 0.0.0.0:8000` 부팅 정상
- Vite dev server `localhost:5173` 정상
- Electron 앱 정상 렌더링, WebSocket 연결 성공
- Start Session 시 v2 모드 rule-based 교사로 시뮬레이션 진행 정상
- HANDOFF.md에 기록된 "V2 흰 화면" 버그는 재현되지 않음 (Phase 6 observable-boundary 작업에서 자연스럽게 해소된 것으로 추정)

### 8.4 E2E 성능

```
규칙 기반 (실험용):     20명 × 950턴 = 0.6초/학급, 100학급 < 1분
Codex CLI (시연용):     10명 × 5턴 = ~150초 (학생당 ~15초)
로컬 LLM (예상):        20명 × 950턴 × ~15초/턴 ≈ 12시간 (장거리 실험 전용)
```

---

## 9. 현재 상태 평가

ADAS는 현재 단순한 초기 프로토타입이 아니라, 에이전트 기반 교실 시뮬레이션으로서 핵심 도메인과 실험 파이프라인이 한 번 다 구현되고 실증 결과까지 확보된 상태다.

정리하면 다음 수준까지 올라와 있다.

- **학생 에이전트**: 18개 프로파일, dimensional delta 모델, 감정 8차원, `effective_params` 정직성 확보
- **교사 에이전트**: 5-Phase 전략, 7차원 감정 rule-based, Case Base + Experience Base, 가설 검증 루프, 선택적 early-ID 정책
- **시간 축/환경**: 950턴 교실, 6개 아키타입, SituationalModulator 9개 이벤트, 관계 그래프, 부분 관찰
- **메모리 시스템**: 2-tier (Case Base + Experience Base), cross-class metrics, JSON 파일 기반 persistence
- **실험 인프라**: growth comparison harness, paired stats, robustness sweep
- **시각화**: 6-panel matplotlib figures
- **LLM 통합**: 4개 백엔드(local/codex/claude/mock), factory, prompt style 분기, runner CLI, memory persistence, wiring 버그 수정
- **데스크톱 앱**: Electron + React + Phaser, WebSocket 실시간 스트리밍, 캐릭터 애니메이션
- **실증 결과**: 30학급 rule-based 실험으로 policy 정책 효과 확인 (sensitivity +6.7%p, latency −2.7턴, PPV 보존)
- **테스트**: 854개, 전부 pass

---

## 10. 남은 리스크 / 남은 우선순위

- **로컬 LLM 교사 실험 미실시**: 백엔드 배선 완성, 2026-04-15 이관 후 smoke run 예정
- **프롬프트 품질**: `local_clean` 스타일은 구조적 검증만 완료, 실제 로컬 모델 JSON 준수율 미측정
- **SQLite 이관**: 현재는 JSON 파일 기반, teacher memory와 LLM response cache를 SQLite로 이관 예정
- **Electron 앱 LLM 연결**: `server.py:595`에서 여전히 `llm_backend=None`, 앱 경로에 LLM 교사 옵션 배선 필요
- **통계적 유의성 강화**: scipy 도입 → Wilcoxon signed-rank test, 또는 더 많은 학급 실험
- **논문/발표**: Methods/Results/Discussion 초안 작성 필요
- **다학급 학생 LLM**: 현재 학생은 rule-based, 시연용 LLM은 있으나 실험용 미통합

---

## 11. 공통 기술 방향

작업 전반에 일관된 방향이 있다.

- **Observable boundary 엄격화**: 교사는 학생 latent를 직접 읽을 수 없도록 관찰 경계를 명시화 — 시뮬레이션 결과의 신빙성에 직결됨
- **Rule-based 우선, LLM 선택적**: 기본은 결정론적/재현 가능한 rule-based, LLM은 opt-in
- **Slice 기반 개발**: 한 번에 한 개의 잘 정의된 변경만 도입, 매 slice 테스트 + 리포트 동반
- **Backward compatibility**: 새 기능은 전부 opt-in (기본값이 기존 동작과 동일)
- **Explicit contracts**: config/metadata 구조화(`PhaseConfig`, `_ranges`, `_sources`, `PROFILE_DELTAS`) → autoresearch 연계 가능
- **문헌 기반 파라미터**: 모든 baseline/delta에 `_sources` 주석, 한국 임상/교육 연구 우선

---

## 12. 이번 기간의 성과 요약

### 12.1 구현 성과

- Phase 1A~1C, 2A/2B, 3A/3C, 4.5 Autoresearch, Phase 6 slices 1-35 완주
- 코드 규모: 시뮬레이션 코어 ~5,000줄, 실험 스크립트 ~1,500줄, 테스트 854개
- 18개 프로파일 + 6개 아키타입 + 9개 상황 modulator + 5-Phase 교사 전략 + 2-tier memory 완비

### 12.2 안정화 성과

- Phase 6 observable-boundary 통과로 시뮬레이션 결과 신빙성 확보
- `effective_params` 정직성 확보 — 이름과 실제 동작 일치
- 메모리 wiring 버그(stale reference) 발견 및 수정 — LLM 경로 블로커 해소

### 12.3 기술적 성과

- 에이전트 기반 시뮬레이션에서 관찰 경계와 feedback 지연을 명시화
- Growth curve 실험 파이프라인 end-to-end 완성 (harness → execute → plot → stats)
- 로컬 LLM 통합 인프라 완비 (backend + factory + prompt style + persistence + wiring)
- 테스트 854개 규모, 모두 pass

### 12.4 실증 성과

- 30학급 rule-based 실험 실행 완료
- "교사가 학급 반복을 통해 판별 속도가 향상된다"는 핵심 가설에 대한 **방향적 긍정 근거 확보**
- Sensitivity +6.7%p, identification turn −2.7턴, PPV 보존이라는 논문/발표에서 주장 가능한 결과

### 12.5 제품 관점 성과

- 단순한 UI 구현이 아니라 실제 사용자 흐름(시뮬레이션 시작 → 실시간 관찰 → 학급 완료 → 성장 추적)이 이어지는 상태
- 학생 20명 × 950턴 시뮬레이션이 실제로 돌아가고 UI로 확인 가능
- 실험 실행 → 시각화 → 통계 분석까지 CLI 한 줄로 재현 가능

---

## 13. 결론

이번 기간의 작업은 ADAS 프로젝트에서
**"아이디어를 코드로 옮기는 단계"부터 "시뮬레이션이 자기 설계 의도대로 정직하게 돌아가며, 실증 결과를 뽑는 실험 파이프라인을 갖추는 단계"까지 한 번에 완주한 작업**이었다.

정리하면 다음과 같다.

- Phase 1~3에서 dimensional delta 모델 + 950턴 교실 + 5-Phase 교사 전략 + 3-tier calibration loss까지 기반 구현
- Phase 4~4.5에서 2-tier memory + autoresearch 인프라 완성
- Phase 6 slices 1-19에서 관찰 경계, feedback 지연, 노이즈, perceive 루프 같은 구조적 부채를 모두 상환
- Phase 6 slices 20-27에서 실험 파이프라인(harness → visualization → stats) 완성
- 30학급 실험 실행으로 "교사 성장"에 대한 첫 실증 결과 확보
- Phase 6 slices 28-35에서 로컬 LLM 이관을 앞두고 백엔드/프롬프트/메모리 persistence/wiring 버그까지 전부 사전 정비

즉, 이번 보고서는 단순 구현 목록이 아니라,
내가 ADAS 프로젝트 전체에서 **설계 정직성, 실험 재현성, 실증 결과, LLM 전환 준비까지 포함해 실제로 끝까지 다듬은 작업**을 정리한 문서다.

---

## 부록 — 병행 프로젝트 현황

이 보고서의 본문은 ADAS에 초점을 맞췄지만, 같은 기간에 다른 프로젝트도 병행했다. 일부는 이전(2026-03-30) 보고서에 이미 정리된 바 있어 중복을 피한다. 해당 보고서에 기록되지 않은 후속 작업만 여기 간단히 적는다.

### ToneMate (2026-03-31 이후)

- 2026-03-30자 이전 보고서에서 다룬 내용: 백엔드-프론트엔드 API 계약 정렬, draft/session/feedback/delivery/retrospect 체인 연결, 전송 전략 보완, 편집 전송 플로우 보완, 권한 경계 보완, STT 제거 및 텍스트 입력 전환, 실패 시나리오 UX 보완
- **그 이후 후속 작업 (2026-03-31)**:
  - iOS 26 시뮬레이터에서 `flutter_contacts` 플러그인 크래시 이슈 확인
  - 대응으로 `DeviceContactsScreen`을 임시 placeholder 로 축소: "기능 준비 중" 메시지 + 카카오톡 대화 가져오기 경로로 유도 (`/import-chat`)
  - 홈 스크린 관련 소규모 조정
- 이후로는 ADAS 쪽에 집중하느라 ToneMate는 placeholder 유지 상태. 향후 호환 가능한 contacts 플러그인으로 교체 예정.

### 검증

- ToneMate 저장소는 별도 git 초기화 상태 (commits 없음) — 실제 작업은 파일 수정 기준으로 확인
- ADAS 검증은 본문 8장 참조
