# ADAS 구현 현황

> **최근 업데이트 (2026-04-14)**: Phase 6 slices 1-35 완료 (Phase 1A~1C, 2A+2B, 3A+3C, 4.5 Autoresearch, 6 realism + honesty pass). **800+ tests passed**.
>
> **Phase 1-3 (기반)**
> - Phase 1A: 공존 프로파일 7개 추가 (18개 총)
> - Phase 1B: 교사 감정 7차원 rule-based (이벤트 카탈로그 15개) + 프로파일 `_sources`/`_ranges` 완성
> - Phase 1C: `.harness/student_parameter_ranges.yaml` (autoresearch 탐색 공간)
> - Phase 2A/2B: `situational_modulator.py` + `ClassroomEnvironmentV2.step()` 통합
> - Phase 3A/3C: naturalness/epidemiology targets + `src/calibration/loss.py` (3-tier)
>
> **Phase 4.5 — Autoresearch/Calibration**: constraint-aware filtering, held-out validation, orchestrator core
>
> **Phase 6 — Realism + Honesty Pass (slices 1-19)**
> - slices 1-4: observable-only teacher observations + explicit feedback payload
> - slices 5-6: teacher perception noise + delayed hypothesis-test feedback
> - slices 7-8: behavior-derived class climate + incident load
> - slices 9-14: retrieval noise config + checkpoint/resume
> - slices 15-19: observable turn events → student perceive loop, behavior-derived mood, observable-only StudentSummary
>
> **Phase 6 — Backend/Experiment Pipeline (slices 20-35)**
> - slice 20: `effective_params` honesty pass (vision_r/importance_trigger 실제 작동)
> - slice 21: memory-aware early-identification policy (optional, conservative)
> - slice 22: 구조화된 early-ID 메타데이터 (real-loop 통합)
> - slice 23: `ClassMetrics` 클래스 수준 early-ID 집계
> - slice 24: `suspicion_to_identification_delta` per-student/class 메트릭
> - slice 25: `scripts/run_growth_comparison.py` (baseline vs policy 비교 harness)
> - slice 26: `scripts/plot_growth_comparison.py` (6-panel 시각화)
> - slice 27: `scripts/analyze_growth_comparison_stats.py` (paired sign test)
> - slice 28: Phase 1B 프로파일 완성도 (`_ranges` 18개 profile)
> - slice 29: `scripts/run_robustness_checks.py` (sensitivity ±20% + cross-scenario)
> - slice 30: `src/llm/local_llm.py` (OpenAI-compatible 로컬 LLM 백엔드)
> - slice 31: `create_backend(name, **kwargs)` factory/dispatch
> - slice 32: 실험 runner `--teacher-backend` CLI 플래그
> - slice 33: 교사 프롬프트 `local_clean` 스타일 (27B 로컬 모델용)
> - slice 34: `TeacherMemory.save()`/`load()` (JSON 파일 persistence)
> - slice 35: 메모리 wiring 버그 수정 (`orch.teacher_llm.memory` stale reference)
>
> **30학급 실험 완료** (2026-04-12): rule-based 비교, policy arm sensitivity +6.7%p, identification turn −2.7턴, PPV 보존.

## 한 줄 요약

한국 초등학교 교실에서 AI 교사 에이전트가 20명 학생(정상+ADHD+혼동 프로파일)을 1년(950턴) 동안 관찰하며, 메모리를 축적해 ADHD를 판별하고 케어하는 시뮬레이션. Electron 데스크톱 앱으로 실시간 시각화.

---

## 핵심 모듈 (새로 만든 것)

### 1. 학생 인지 에이전트 (`src/simulation/cognitive_agent.py`, ~580줄)

Stanford Generative Agents의 인지 아키텍처를 채택:

```
perceive(지각) → retrieve(회상) → reflect(반성) → plan(계획) → act(행동)
```

ADHD는 별도 코드가 아니라 **같은 아키텍처의 파라미터 차이**로 모델링:

| 파라미터 | 정상 | ADHD 부주의 | ADHD 과잉행동 | 불안 | ODD |
|---|---|---|---|---|---|
| att_bandwidth (주의 폭) | 3 | 1 | 2 | 2 | 2 |
| impulse_override (충동) | 0.05 | 0.15 | 0.40 | 0.10 | 0.35 |
| plan_consistency (계획 이행) | 0.90 | 0.50 | 0.40 | 0.75 | 0.40 |
| retention (기억 유지) | 5 | 2 | 3 | 4 | 4 |

**18개 프로파일** (2026-04-10 업데이트, §25/§25.11 Dimensional delta 구조):

**단일 (9개)**: normal_quiet, normal_active, adhd_inattentive, adhd_hyperactive_impulsive, adhd_combined, anxiety, odd, gifted, sleep_deprived

**공존 (6개)**: adhd_i_plus_anxiety, adhd_h_plus_odd, adhd_c_plus_odd, adhd_i_plus_ld, adhd_plus_depression, anxiety_plus_depression

**감별진단 distractor (3개)**: asd_like, depression, learning_disorder
(교사가 ADHD 와 감별해야 하는 혼동 프로파일)

**구조**: `normal_quiet` 을 baseline 으로 두고, 다른 프로파일은 `BASE_COGNITIVE + delta` / `BASE_EMOTIONAL + delta` / `BASE_OBSERVABLE + delta` 로 표현. 공존 프로파일은 `_base` field 로 재귀적 합성 + 명시적 interaction term.

```python
PROFILE_DELTAS["adhd_i_plus_anxiety"] = {
    "_base": ["adhd_inattentive", "anxiety"],  # 두 delta 합산
    "cognitive": {"plan_consistency": +0.05, ...},  # 추가 interaction
    "emotional": {"shame": +0.05, ...},
    "observable": {"distress_level": +0.03},
    "_sources": "MTA 1999; Jensen 2001 ADHD-anxiety comorbid"
}
```

**관찰 state 프로파일별 분리** (이전 버그 수정): 모든 학생이 동일한 `{attention: 0.65, compliance: 0.72}` 로 시작하던 문제 해결. 이제 ADHD-combined 는 `attention=0.25, compliance=0.43` 으로 시작.

3차원 감정 모델: anxiety, anger, excitement (8→3 축소. 제거: frustration·shame·loneliness=행동출력 0, self_esteem→task_initiation_delay·trust_in_teacher→plan_consistency 로 효과 이전)

### 2. 950턴 교실 환경 (`src/simulation/classroom_env_v2.py`, ~800줄)

- 1턴 = 1교시, 1학급 = 190일 × 5교시 = 950턴 (한국 교육법 기준)
- 20명 학생: ADHD 6-11% + 혼동 프로파일 15-25% (랜덤)
- 학생 간 관계 그래프: 친구/갈등/좌석인접
- InteractionEngine: 또래 전염, 수다, 충돌, 괴롭힘, 도움
- 부분 관찰: 교사는 턴당 3명만 상세 관찰, 나머지는 겉보기만
- 시간 경과: 교과목 변경, 쉬는시간/체육 시 운동장 이동
- 관리 기준: compliance ≥ 0.80 for 25턴 연속 (재발 시 리셋)

### 2b. Situational Modulator (`src/simulation/situational_modulator.py`, ~340줄, 신규 2026-04-10)

**문헌 기반 상황 modulation** — 교실 역동을 현실적으로 만들기 위한 환경 요인:

- **학기 주기**: 시험 주간 (Cassady 2002), 학기 말 피로 (Lim 2016)
- **일중 리듬**: Post-lunch dip (Folkard 1975), 오후 집중 저하 (Schmidt 2007)
- **신학기 적응**: 100턴 선형 감쇠 (Akos & Galassi 2004)
- **또래 갈등**: Social pain (Eisenberger 2003) → 교실 파급
- **대체 교사**: 규율 감소 + ODD 증폭 (Clifton 1987)
- **발표 이벤트**: 불안 학생 증폭 (Stein 1996)
- **날씨**: 작은 효과만 (Denissen 2008 r ≈ 0.1 신중 사용)

**구조**:
```python
class SituationalModulator:
    def compute_modulation(turn, period) -> ModulationVector:
        # 1. Academic cycle (exam weeks, fatigue)
        # 2. New semester adaptation
        # 3. Diurnal rhythm
        # 4. Active events (peer conflict, substitute, presentation)
        # → additive composition + clamping
```

**ModulationVector 필드**:
- Global emotion shifts: anxiety, frustration, excitement, loneliness
- Global cognitive: attention, compliance
- Class-level: stress, disruption (max, not sum)
- Amplification factors: adhd / anxiety / odd (multiplicative)
- Event flags: exam_week, substitute_teacher, peer_conflict, ...

**Default Korean K-6 schedule**: 950턴 내 자동 배치된 4 peer conflicts, 2 substitute days, 4 presentations.

25 tests passed.

### 2d. Calibration Loss (`src/calibration/loss.py`, 신규 2026-04-10)

**3-Tier 구조** (정리.md §25.13):
- **Tier 1 (60%)**: Naturalness patterns — 논문 15개 패턴 매칭
- **Tier 2 (40%)**: Epidemiological anchors — 유병률, 공존률 등
- **Tier 3 (held-out)**: 검증용 — loss 에 포함 안 됨

**핵심 함수**:
```python
compute_combined_loss(
    sim_result,
    naturalness_targets,   # Tier 1
    epidemiology_targets,  # Tier 2
    interaction_terms,     # comorbidity sparsity
    naturalness_weight=0.6,
    epidemiology_weight=0.4,
    sparsity_weight=0.05,
) -> LossResult  # 스칼라 + 브레이크다운
```

**Distance 함수**:
- `range_distance(value, (lo, hi))`: 범위 안이면 0, 밖이면 정규화 거리
- `point_distance(value, target, tolerance)`: 점 target + 허용 오차

**Sparsity penalty**: comorbidity interaction term 에 대한 L1 정규화 → 자동 스파스화

**LossResult 객체**: total scalar + per-component + per-target 상세 (최악 5개 표시)

20 tests passed.

### 2c. Parameter Range YAML (`.harness/student_parameter_ranges.yaml`, 신규)

Autoresearch 탐색 공간 정의:
- 10개 cognitive 파라미터 range
- 8개 emotional 파라미터 range
- 프로파일별 delta range (ADHD-I, ADHD-H, ...)
- Emotion decay rates (프로파일별)
- Event reactivity coefficients
- Emotion→cognition coupling
- Comorbidity interaction sparsity weights
- Hard constraints (단조성, 우선순위)

**학생 (autoresearch 대상) vs 교사 (문헌 고정)** 의 명확한 구분. 학생 모든 범위는 literature-informed.

### 3. 오케스트레이터 v2 (`src/simulation/orchestrator_v2.py`, ~700줄)

**교사 감정 — 7차원 rule-based (2026-04-10 업데이트, §25.11)**:

```python
BASE_TEACHER_EMOTIONAL = {
    "emotional_exhaustion":    0.42,  # MBI-EE / 이영만 2012
    "depersonalization":       0.30,  # MBI-DP / 조환이 2016
    "personal_accomplishment": 0.72,  # MBI-PA / 한국 교사
    "self_efficacy":           0.72,  # TSES / Klassen 2009
    "empathy":                 0.76,  # Goroshit 2016
    "patience":                0.72,  # Schnitker 2012
    "job_stress":              0.62,  # Kyriacou / 한국
}
```

**설계 원칙**:
- 초기값: 한국 교사 normative data 에서 **문헌 고정** (autoresearch ❌)
- 변화: **100% rule-based** 이벤트 카탈로그 (15개) — 각 계수 논문 인용
- LLM: 감정 **읽기만** (의사결정 context 로), 감정 **쓰기는 금지**
- 재현성: 동일 입력 → 동일 출력 보장

**이벤트 카탈로그** (각 method 에 논문 출처):
```python
on_student_incident(n)           # Klassen & Chiu 2010
on_identification_success()      # TSES (Tschannen-Moran 2001)
on_identification_failure()      # Maslach 1981
on_chaotic_mood() / on_calm_mood()  # Hargreaves 2000
daily_recovery()                 # Maslach burnout curves
on_semester_start()              # Klassen 2009 (reset)
on_public_correction()           # Cognitive dissonance
on_empathic_intervention_success()  # Goroshit 2016
on_major_student_crisis()        # Acute stress
on_parent_communication_success()   # Self-efficacy boost
on_conflict_resolved()           # Personal accomplishment
on_exam_week_start()             # Klassen stress
apply_burnout_acceleration()     # EE > 0.7 임계점 (Maslach 임상)
```

**책임 분리** (순환 회피 핵심):
- 교사 감정 = 결정론적 (rule-based, reproducible)
- 교사 결정 = 학습 (LLM + Case Base + Experience Base)
- 두 시스템이 명확히 분리되어 ablation 에서 각 기여도 측정 가능

**Legacy 호환**: `frustration`, `empathy_capacity` property alias 로 기존 코드 호환.

5-Phase 교사 전략:
```
Phase 1 (Turn 1-100):   순수 관찰. 학생 이름 익히기, 전체 훑기
Phase 2 (Turn 100-300): 스크리닝. 의심 학생 집중 관찰
Phase 3 (Turn 300-475): 판별. confidence ≥ 0.85 & 10회+ 관찰 시 DSM-5 리포트
Phase 4 (Turn 475-700): 케어. 개입 전략 실행
Phase 5 (Turn 700-950): 유지 + 재발 감지 + 재개입
```

`stream_class()` 제너레이터로 실시간 스트리밍:
```python
for event in orch.stream_class():
    if event["type"] == "turn": 
        # 즉시 WebSocket으로 전송
    elif event["type"] == "class_complete":
        # 학급 종료, 메트릭 포함
```

### 4. 교사 메모리 (`src/simulation/teacher_memory.py`, ~310줄)

Agent Hospital의 MedAgent-Zero 스타일:
- **Case Base**: 관찰 기록 저장, 코사인 유사도 RAG 검색
- **Experience Base**: 성공/실패에서 원칙 추출, 검증 후 보존
- 학급이 바뀌어도 메모리 유지 → 갈수록 판별 정확도 향상

### 5. 판별 리포트 (`src/eval/identification_report.py`, ~280줄)

DSM-5 기준 한국어 매핑:
- 부주의 9개 + 과잉행동-충동 9개 = 18개 기준
- 관찰된 행동을 DSM-5 기준에 매핑
- 6개 이상 충족 시 판별 리포트 생성

### 6. 성장 평가 (`src/eval/growth_metrics.py`, ~310줄)

학급별 성장 추적:
- 민감도, 특이도, PPV, F1
- 한국 벤치마크와 비교 (K-ARS 민감도 >94%, PPV 0.19)
- 트렌드 분석 (학급이 진행될수록 개선되는지)

### 7. 상호작용 로그 (`src/simulation/interaction_log.py`, ~250줄)

모든 이벤트 기록:
- 교사→학생, 학생↔학생, 환경→학생
- 감정 상태 변화 추적
- 학급별 쿼리, JSON 내보내기
- 학급당 10,000 이벤트 캡 (메모리 보호)

### 8. LLM 연동

| 파일 | 역할 |
|---|---|
| `src/llm/teacher_llm.py` | Claude CLI로 교사 의사결정 (한국어 프롬프트) |
| `src/llm/student_llm.py` | Codex CLI로 학생 감정 반응 (시연용) |
| `src/llm/mock_backend.py` | 테스트용 mock LLM |

설정으로 전환 가능:
```yaml
student_backend: "rule"       # rule | codex_cli | claude_cli | local_llm
teacher_backend: "claude_cli" # rule | codex_cli | claude_cli
```

---

## Electron 데스크톱 앱

### 구조
```
app/
├── electron/main.js          # Python 백엔드 자동 실행, venv 탐색
├── backend/server.py         # FastAPI WebSocket (classic/multi/v2 모드)
└── renderer/src/
    ├── App.jsx               # 모드 분기, 상태 관리
    └── components/
        ├── ClassroomView.jsx  # Phaser 통합 학교 맵 + 캐릭터 스프라이트
        ├── StudentGrid.jsx    # 20명 학생 상태 그리드
        ├── StatePanel.jsx     # 집중 학생 상태 바 + 교사 행동
        ├── ChatLog.jsx        # 상호작용 로그 (한국 이름, 색상)
        ├── ControlPanel.jsx   # 모드/속도/일시정지 제어
        └── GrowthPanel.jsx    # 성장 트렌드 표시
```

### 학교 맵 (Phaser 3)
```
┌─────────────────────┬──────────┐
│    교실 (Classroom) │ 교무실   │  ← 픽셀아트 이미지
│                     │ (Office) │
├─────────────────────┴──────────┤
│           복도 (Hallway)       │
├────────────────────────────────┤
│        운동장 (Playground)     │  ← 픽셀아트 축구장
└────────────────────────────────┘
```

- 캐릭터 21명 (교사 1 + 학생 20) 걷기 애니메이션
- 시나리오에 따라 교실↔운동장 이동
- 활성 공간 하이라이트, 비활성 공간 딤

### WebSocket 프로토콜 (v2 모드)

```
Client → Server: {"type": "start_session", "mode": "v2", "n_students": 20}

Server → Client:
  1. {"type": "new_class", "class_id": 1, "students": [...], ...}
  2. {"type": "turn", "turn": 1, "day": 1, "period": 1, "subject": "math", ...}
  3. {"type": "turn", "turn": 2, ...}
  ... (950 turns)
  N. {"type": "class_complete", "metrics": {...}, "reports": [...]}

Client → Server (제어):
  {"type": "pause"}
  {"type": "resume"}
  {"type": "speed", "delay": 0.5}
```

실시간 스트리밍: `stream_class()` 제너레이터가 턴마다 yield → 서버가 즉시 전송.
pause/resume는 실제 시뮬레이션을 멈추고 재개 (replay가 아님).

---

## 테스트

```
총 157 passed, 0 failed (repo-local .venv)

test_v2_server.py:       10개 — 진짜 async 서버 WebSocket 테스트
test_v2_orchestrator.py: 11개 — 오케스트레이터 스트리밍/Phase/트리밍
test_v2_integration.py:   8개 — v2 모듈 통합
test_integration_multi.py: 37개 — v1 다중 학생
test_audit.py:           20개 — 아키텍처 감사
기타:                    71개 — 기존 단위 테스트
```

---

## E2E 성능

```
규칙 기반 (실험용):     20명 × 950턴 = 0.6초/학급, 100학급 < 1분
Codex CLI (시연용):     10명 × 5턴 = ~150초 (학생당 ~15초)
```

---

## 기존 모듈 (v1, 유지)

| 파일 | 역할 |
|---|---|
| `src/environment/child_env.py` | 단일 학생 Gymnasium 환경 (Classic 모드) |
| `src/environment/transition_constraints.py` | 논문 기반 행동 전환 제약 |
| `src/reward/reward_function.py` | 다차원 보상 함수 |
| `src/agent/ppo_agent.py` | PPO 에이전트 |
| `src/agent/baselines.py` | Random/RuleBased/SingleAction 베이스라인 |
| `src/simulation/multi_student_env.py` | v1 다중 학생 환경 (Multi 모드) |
| `src/simulation/orchestrator.py` | v1 오케스트레이터 |

---

## 실행 방법

```bash
# 백엔드 시작
cd adas
.venv/bin/python app/backend/server.py

# 프론트엔드 + Electron
cd app/renderer && npx vite --port 5173
cd app && npx electron .

# 테스트
.venv/bin/python -m pytest tests/ -q

# E2E 테스트 (mock LLM, 2학급)
.venv/bin/python scripts/run_e2e_test.py

# Codex CLI 데모 (10명 × 5턴)
.venv/bin/python scripts/run_codex_demo.py
```

---

## 데이터 소스

`data.md`에 전체 정리. 한국 전용:
- 역학 연구 8편 (49,573명 유병률 등)
- 교실 행동 관찰 6편 (메타분석)
- 한국 평가 도구 11개 (K-ARS, K-CBCL 등)
- 개입 연구 10편
- 사례연구 44편

---

## Phase 2 교사 에이전트 고도화 (구현 완료)

### 가설-검증 루프 (HypothesisTracker)
- 즉시 판별 대신 `suspicious → 3종 개입 테스트 → 반응 패턴 분석 → 감별`
- empathic_acknowledgment → distress 감소 크면 → 불안장애 가능성
- break_offer → 행동 개선 → ADHD 가능성
- firm_boundary → 반항 증가 → ODD 가능성
- 3종 테스트 완료 후에만 판별 허용

### 지연 피드백
- 판별 후 30%만 ground truth 확인 가능 (feedback_rate=0.30)
- 70%는 맞았는지 모름 → Experience Base 수정 느려짐

### 메모리 노이즈 + Decay
- retrieval_noise=0.20 (20% 검색 실패)
- memory_decay_rate=0.99 (오래된 기억 감쇠)
- 원칙 승격: 7회+ 성공, 3학급+ 필요 (principle_promotion_threshold)

### 교실 아키타입 6종
quiet_structured / chaotic / exam_period / high_ses / low_ses / mixed_grade
- 학급마다 다른 환경 → 과거 경험이 100% 통하지 않음

### 교사 감정 모델 (TeacherEmotionalState)
patience / empathy_capacity / frustration / bias
- 번아웃 시 판별 임계값 0.8배 (더 공격적 → 오판 증가)
- 일일 회복 (period=1에서 patience += 0.03)

### AUPRC + Macro-F1
- 3분류 Macro-F1 (정상 / ADHD / 혼동 프로파일)
- PR 곡선 면적 근사

### 메모리 기반 의사결정 연결
- Case Base prior: 유사 과거 사례의 ADHD 비율로 suspicious 점수 조정
- Experience Base: 원칙이 suspicious 점수에 ±0.2 배율 적용
- 전략 선택: 유사 ADHD 학생에게 효과적이었던 전략 우선 사용
- was_adhd 라벨링: 판별 확인 시 Case Base 레코드에 소급 태깅
- 경험 기반 판별 임계값: labeled records가 쌓일수록 임계값 0.40→0.20 하향

---

## LLM 교사 연동 (구현 완료)

### Codex CLI 교사 (Teacher = LLM)
- `orchestrator_v2.py`에 `_decide_action_llm()` 구현
- 한국어 프롬프트에 메모리 컨텍스트 포함:
  - 학생 관찰 (전원 행동 + ADHD 점수)
  - Case Base 유사 사례 (과거 ADHD 확인된 학생과의 유사도)
  - Experience Base 원칙 (학습된 감별 규칙)
  - Phase 안내 (현재 단계에서 할 일)
- `generate_raw()`: state 스키마 없이 raw JSON 응답
- 폴백: LLM 오류 시 rule-based로 자동 전환

### 테스트 결과 (10명, 20턴)
```
Turn  1: [observe] S01 — "Phase 1 초기 관찰 단계이므로..."
Turn  2: [observe] S07 — S07만 유일하게 'seems_inattentive' 징후
Turn  9: [observe] S07 — "S07만 유일하게 'seems_inattentive' 행동이 보여"
...
속도: 턴당 ~16초 (Codex CLI)
```

### 실험 구조
```
비교군 1: rule-based (no memory)     ← 하한선
비교군 2: rule-based (case base only)
비교군 3: rule-based (full memory)
비교군 4: LLM teacher (full memory)  ← Agent Hospital 스타일
비교군 5: LLM teacher + LLM students ← 상한선 (향후)
```

---

## 테스트 현황

```
총 185 passed (repo-local .venv)

test_v2_server.py:          10개 — async WebSocket 서버
test_v2_orchestrator.py:    11개 — 스트리밍/Phase/트리밍
test_v2_integration.py:      8개 — v2 모듈 통합
test_integration_multi.py:  37개 — v1 다중 학생
test_audit.py:              20개 — 아키텍처 감사
test_phase2.py:             11개 — Phase 2 전용
test_llm_teacher_wiring.py: 13개 — LLM 교사 프롬프트/파싱
기타:                       75개 — 기존 단위 테스트
```

---

## 실험 결과 (30학급 Ablation)

| 그룹 | Sens | PPV | F1 |
|---|---|---|---|
| no_memory | 0.818 | 0.209 | 0.333 |
| case_base_only | 0.808 | 0.220 | 0.346 |
| full | 0.808 | 0.220 | 0.346 |
| LLM teacher | (테스트 중) | | |

Case Base가 PPV를 5% 개선. LLM 교사는 메모리 컨텍스트를 자유롭게 해석할 수 있어 더 큰 성장 기대.
